#!/usr/bin/env bash
set -e
if [[ ! -d "/home/cloudglides/Snugly/firmware" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/cloudglides/Snugly/firmware")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/cloudglides/Snugly/firmware" true
